import PromoCards from "./PromoCards";

export const dynamic = "force-dynamic";

// Screenshot-ready IG story cards of real (anonymized) ticket purchases.
// Open on your phone, screenshot a card, post to your story.
export default function PromoPage() {
  return <PromoCards />;
}
